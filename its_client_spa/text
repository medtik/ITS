Thêm type cho location
    vd: restaurant, bus stop, hotel
Thêm areaId cho plan
    từ giờ 1 plan sẽ gắn liền với 1 khu vực
    thêm field này vào 1 số request
        api/group/details
        api/plan/details
        api/featuredTrip
        api/MyPlans
        api/PostPlan

GET api/location thêm tính năng để dùng lại cho client
    filter theo khu vực
    filter theo type





